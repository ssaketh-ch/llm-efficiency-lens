# Archive

This folder keeps older or narrow scripts that are no longer the primary workflow.

## Contents

- `run_quality_eval_mmlu_only.sh`: earlier quality runner focused on MMLU-style evaluation.

Use `scripts/run_quality_eval.sh` for the current quality workflow.
